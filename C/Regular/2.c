// How to print a semicolon(;) without using semicolon in C/C++?

#include <stdio.h> 
int main() 
{ 
    // ASCII value of semicolon = 59 
    if (printf("%c\n", 59)) 
    if (putchar(59)) 
    { 
    } 
    return 0; 
} 
